import { StyledButtonContainer } from './style';

const ButtonContainer = (children: JSX.Element) => {
  return <StyledButtonContainer>{children}</StyledButtonContainer>;
};

export default ButtonContainer;
